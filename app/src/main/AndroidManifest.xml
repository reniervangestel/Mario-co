<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.renier.marioco" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >


        <uses-permission android:maxSdkVersion="19" android:name="android.permission.INTERNET"/>
        <!-- Splash screen -->
        <activity
            android:name="com.example.renier.marioco.Splashscreen"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Black.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main activity -->
        <activity
            android:name="com.example.renier.marioco.MainActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.example.renier.marioco.ServiceScherm"
            android:label="@string/title_activity_service_scherm" >
        </activity>
        <activity
            android:name="com.example.renier.marioco.AanvraagScherm"
            android:label="@string/title_activity_aanvraag_scherm" >
        </activity>
        <activity
            android:name="com.example.renier.marioco.Preferences"
            android:label="@string/title_activity_preferences" >
        </activity>
    </application>

</manifest>
